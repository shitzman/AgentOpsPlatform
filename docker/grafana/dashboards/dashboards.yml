# Grafana 自动加载的 Dashboard 配置
apiVersion: 1

providers:
  - name: 'AgentOps'
    orgId: 1
    folder: 'AgentOps'
    type: file
    disableDeletion: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
